import { Viewport } from 'next'

const viewport: Viewport = {
  themeColor: '#FF5A5F',
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
}

export default viewport
